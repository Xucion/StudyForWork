# SQL基础

## SQL分类

| 分类    | 全称                                   | 中文说明                                                     | 核心操作/命令                                               | 一句话记忆             |
| ------- | -------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------- | ---------------------- |
| **DDL** | **D**ata **D**efinition **L**anguage   | **数据定义语言**，用于定义或修改数据库的结构（库、表、视图、索引等） | `CREATE`, `ALTER`, `DROP`, `TRUNCATE`                       | **管结构的**（盖房子） |
| **DML** | **D**ata **M**anipulation **L**anguage | **数据操作语言**，用于对表中的**数据**进行增、删、改操作     | `INSERT`（增）, `UPDATE`（改）, `DELETE`（删）              | **管内容的**（搬家具） |
| **DQL** | **D**ata **Q**uery **L**anguage        | **数据查询语言**，用于从表中查询和检索数据                   | `SELECT`                                                    | **查数据的**（看家具） |
| **DCL** | **D**ata **C**ontrol **L**anguage      | **数据控制语言**，用于定义数据库的权限、访问控制和事务       | `GRANT`（授权）, `REVOKE`（回收权限）, `COMMIT`, `ROLLBACK` | **管权限的**（配钥匙） |

聚合函数

​	SELECT 聚合函数(字段列表) FROM 表名;

分组查询（GROUP BY）

​	SELECT 字段列表 FROM 表名 [WHERE条件] GROUPBY 分组字段名 [HAVING **分组后过滤条件**];

排序查询（ORDER BY）

​	SELECT 字段列表 FROM 表名 ORDER BY 字段1 排序方式1, 字段2 排序方式2;

​	（先以字段1的排序方式排序，相同则以字段2的排序方式排序）

​	排序方式：ASC 升序（默认）；DESC（降序）

分页查询（LIMIT）

​	SELECT 字段列表 FROM 表名 LIMIT 起始索引, 查询记录数

​	起始索引从0开始，起始索引 = （查询页码 - 1）* 每页显示记录数；如果查询的是第一页的数据，起始索引可以省略，直接写limit 10。

DQL-执行顺序	![78884397798](C:\Users\Administrator\Desktop\study\八股\images\1788843977980.png)

外键约束：![78884557580](C:\Users\Administrator\Desktop\study\八股\images\1788845575807.png)

## 关系型数据库vs非关系型

**ACID vs BASE**

​	关系型数据库支持 ACID 即原子性，一致性，隔离性和持续性。

​	NoSQL 采用更宽松的模型 BASE ， 即基本可用，软状态和最终一致性。

**扩展性对比**

​	NoSQL数据之间无关系，这样就非常容易扩展，也无形之间，在架构的层面上带来了可扩展的能力。比如 redis 自带主从复制模式、哨兵模式、切片集群模式。

​	相反关系型数据库的数据之间存在关联性，水平扩展较难 ，需要解决跨服务器 JOIN，分布式事务等问题。

## 数据库三大范式

**1NF**：数据库表的每一列都是不可分割的原子数据项。

**2NF**：在1NF的基础上，非码属性必须完全依赖于候选码。第二范式需要确保数据库表中的每一列都和主键相关，而不能只与主键的某一部分相关（主要针对联合主键而言）。

**3NF**：在2NF基础上，任何非主属性不依赖于其它非主属性（在2NF基础上消除传递依赖）.。第三范式需要确保数据表中的每一列数据都和主键直接相关，而不能间接相关。

## Mysql连表查询(JOIN列变多)

- **INNER JOIN**：取两个表的**交集**（员工有部门 + 部门有员工）。
- **LEFT JOIN**：**左表全集** + 交集部分。
- **RIGHT JOIN**：**右表全集** + 交集部分。
- **FULL JOIN**：**左表全集 ∪ 右表全集**（并集）。

## Mysql联合查询(UNION，UNION ALL行变多)

## Mysql避免插入重复数据

# 事务

**四大特性ACID**

​	原子性(Atomicity):事务是不可分割的最小操作单元，要么全部成功，要么全部失败。

​	一致性(Consistency):事务完成时，必须使所有的数据都保持一致状态。

​	隔离性(Isolation):数据库系统提供的隔离机制，保证事务在不受外部并发操作影响的独立环境下运行。

​	持久性(Durability):事务一旦提交或回滚，它对数据库中的数据的改变就是永久的。

**并发事务问题**

​	脏读：一个事务读到另外一个事务还没有提交的数据。

​	不可重复读：一个事务先后读取同一条记录，但两次读取的数据不同，称之为不可重复读。

​	幻读：同一个事务内，两次**相同的**查询得到的结果集**行数不一样**。第二次查询多出来了一些"幽灵般"的行。

## **事务隔离级别**

​	从上至下↓，隔离级别越来越高，性能越来越低。

**一：Read uncoommitted**：指一个事务还没提交时，它做的变更就能被其他事务看到；**发生脏读、不可重复读、幻读**

**二：Read coommitted**：指一个事务提交之后，它做的变更才能被其他事务看到；	**发生不可重复读、幻读**

**实现方式**：**MVCC + 写锁（行锁）**

- **写操作**：对修改的行加**行级排他锁**，直到事务提交才释放
- **读操作（快照读）**：每次执行 `SELECT` 时，都**重新生成一个 Read View（读视图）**，只读取已提交的事务修改

**MVCC 在这里的行为**：

- 每行数据会有多个历史版本（通过 undo log 实现）
- `SELECT` 时，只看 `Read View` 里可见的版本（即已提交事务的修改）
- 每次查询都重新生成 `Read View`，所以**同一个事务内，两次查询可能看到不同的数据**（不可重复读）

**结果**：解决了脏读，但会出现不可重复读和幻读。

**三：Repetable Read（默认）**：指一个事务执行过程中看到的数据，一直跟这个事务启动时看到的数据是一致的，MySQL InnoDB 引擎的默认隔离级别；										**发生幻读**

**实现方式**：**MVCC + 行锁（间隙锁 Gap Lock 是重点）**

- **写操作**：加行锁 + **间隙锁（Gap Lock）**，防止其他事务插入数据
- **读操作（快照读）**：**事务开始时生成一个 Read View**，整个事务期间都使用这个视图，不再重新生成

**MVCC 在这里的行为**：

- 事务开启时（第一次 `SELECT`），生成一个全局的 `Read View`
- 之后无论查询多少次，都读这个历史快照，保证**每次读到的数据一致**（可重复读）
- 其他事务的修改（提交或不提交），对当前事务都不可见

**间隙锁（Gap Lock）的作用**：

- 不仅锁定查到的行，还锁定行之间的“间隙”
- 比如 `SELECT * FROM users WHERE age BETWEEN 10 AND 20 FOR UPDATE`，会锁住 age 在 10~20 之间的所有行 + 这个范围的所有空隙
- 其他事务无法在这个间隙里插入新数据，从而**防止幻读**

**结果**：解决了脏读和不可重复读，并且在 MySQL 的 RR 级别下，通过间隙锁也解决了幻读（但标准 SQL 的 RR 级别是允许幻读的，这里是 MySQL 的特殊实现）

**四：Serializable**：会对记录加上读写锁，在多个事务对这条记录进行读写操作时，如果发生了读写冲突的时候，后访问的事务必须等前一个事务执行完成，才能继续执行；							**都不发生**

## 存储引擎：

​	存储引擎就是存储数据、建立索引、更新/查询数据等技术的实现方式。存储引擎基于表，而不基于库，所以称为表类型。

### InnoDB

​	InnoDB是MySQL的默认存储引擎，具有ACID事务支持、行级锁、外键约束等特性。它适用于高并发的读写操作，支持较好的数据完整性和并发控制。

​	如果应用对事务的完整性有比较高的要求，在并发条件下要求数据的一致性，数据操作除了插入和查询之外，还包含很多的更新、删除操作，那么nnoDB存储引擎是比较合适的选择。

​	xxx.idb文件：xxx代表表名，innoDB引擎的每张表都会对应这样一个表空间文件，存储该表的表结构、数据和索引。

![78885479142](C:\Users\Administrator\Desktop\study\八股\images\1788854791423.png)

### MyISAM

​	是Mysql早期默认的存储引擎。特点：不支持事务，不支持外键，支持表锁，不支持行锁，访问速度快。

​	如果应用是以读操作和插入操作为主，只有很少的更新和删除操作，并且对事务的完整性、并发性要求不是很高，那么选择这个存储引擎是非常合适的。

### Memory

​	Memory引擎的表数据是存储在内存中的，由于收到硬件、断电问题的影响，只能做缓存/临时表。特点：内存存放，hash索引（默认）。

# 索引

​	索引(index)是帮助MySQL**高效**获取数据的**数据结构(有序)**。在数据之外，数据库系统还维护着满足特定查找算法的数据结构，这些数据结构以某种方式引用(指向)数据，这样就可以在这些数据结构上实现高级查找算法，这种数据结构就是索引。

​	Mysql对大数据表构建索引，也需要很长时间（构建B+树）。

![78885699815](C:\Users\Administrator\Desktop\study\八股\images\1788856998157.png)

## 索引结构

![78885727852](C:\Users\Administrator\Desktop\study\八股\images\1788857278524.png)

二叉树缺点：顺序插入时，会形成一个链表，性能大大降低。大数据量情况下，层级较深，检索速度慢。

红黑树：自平衡的二叉树，但仍然存在以上问题

B树：多路平衡查找树，以5阶为例，一个节点最多有5个指针，4个key，当key的个数大于4时，B树会向上裂变，取中间的key作为父节点的key

**B+树**：在B+树的数据结构中，所有的元素都会出现在叶子节点。并且叶子节点形成一个单向链表。

在Mysql的索引结构中，叶子节点会形成一个双向链表。

![78888321505](C:\Users\Administrator\Desktop\study\八股\images\1788883215054.png)

**Hash**：不支持范围查询，无法完成排序，查询效率高，通常高于B+树索引

## 为什么InnoDB存储引擎选择B+树索引结构？

**B+Tree vs B Tree：**B+Tree 只在叶子节点存储数据，而 B 树 的非叶子节点也要存储数据，所以 B+Tree 的单个节点的数据量更小，在相同的磁盘 I/O 次数下，就能查询更多的节点。另外，B+Tree 叶子节点采用的是双链表连接，适合 MySQL 中常见的基于范围的顺序查找，而 B 树无法做到这一点。

**B+Tree vs 二叉树：**对于有 N 个叶子节点的 B+Tree，其搜索复杂度为O(logdN)，其中 d 表示节点允许的最大子节点个数为 d 个。在实际的应用当中， d 值是大于100的，这样就保证了，即使数据达到千万级别时，B+Tree 的高度依然维持在 3~4 层左右，也就是说一次数据查询操作只需要做 3~4 次的磁盘 I/O 操作就能查询到目标数据。而二叉树的每个父节点的儿子节点个数只能是 2 个，意味着其搜索复杂度为 O(logN)，这已经比 B+Tree 高出不少，因此二叉树检索到目标数据所经历的磁盘 I/O 次数要更多。

**B+Tree vs Hash：**Hash 在做等值查询的时候效率贼快，搜索复杂度为 O(1)。但是 Hash 表不适合做范围查询，它更适合做等值的查询，这也是 B+Tree 索引要比 Hash 表索引有着更广泛的适用场景的原因

## 索引分类

主键索引：默认创建，只能有一个，PRIMARY

唯一索引：避免同一个表中某数据列中的值重复，UNIQUE

常规索引

全文索引：全文索引查找的是文本中的关键词，而不是比较索引中的值，FULLTEXT

**按照存储形式分类：**聚集索引；二级索引（手动创建的都是二级索引）

![78892346945](C:\Users\Administrator\Desktop\study\八股\images\1788923469456.png)

![78892367647](C:\Users\Administrator\Desktop\study\八股\images\1788923676475.png)

**回表查询**：先走二级索引找到对应的主键值，再根据主键值到聚集索引中找到这一行的行数据。

## InnoDB主键索引的B+树高度为多高？

​	假设一行数据大小为1k，一页中可以存储16行这样的数据。InnoDB的指针占用6个字节的空间，主键为bigint，占用字节数为8。（**页大小**：默认 **16KB** = 16,384 字节）

- 每个索引条目 = 主键(8字节) + 指针(6字节) = **14 字节**
- 每页能存的条目数 = 16,384 / 14 ≈ **1170 个**
- 高度为 n（根节点 + 叶子节点）
  - 根节点（非叶子）有 1170 个指针，指向 1170 个中间节点（n-1次）
  - 每个中间节点又指向 1170 个叶子节点
  - 每个叶子节点存 16 行
  - 总共能存：**$1170^{n-1} × 16$ 行**

联合索引的参数顺序有讲究

## ---性能分析---

### **SQL执行频率**

​	SHOW GLOBAL STATUS LIKE 'Com____'; （七个下划线）。可以提供服务器状态信息。通过如下指令，查看当前数据库的INSERT、UPDATE、DELETE、SELECT的访问频次。

### **慢查询日志**

​	慢查询日志记录了所有执行时间超过指定参数（long_query_time，单位s，默认10s）的所有SQL语句的日志。

​	show variables like 'slow_query_log'; 查看有没有开启。默认没有开启，要在/etc/my.conf中配置。

​	配置完后，重启查看localhost-slow.log。

### **profile详情**

​	通过having_profile参数能够查看Mysql是否支持profile操作。

​	SELET @@have_profile; 查看是否支持，默认关闭。

​	SELECT @@profilng；查看是否开启

​	SET profilng = 1；打开

- show profiles；可以查看每一条sql的耗时基本情况。
- show profile for query query_id；可以查看指定id的sql语句各个阶段的耗时情况。
- show profile cpu for query query_id；可以查看指定id的sql语句cpu的使用情况。

### **explain执行计划**

在 SELECT 语句之前加 EXPLAIN 或者 DESC 命令获取 MySQL 如何执行 SELECT 语句的信息，包括在SELECT 语句执行过程中表如何连接和连接的顺序。

每列的含义？ 

​	**id：**select查询的序列号，表示查询中执行select子句或者是操作表的顺序

​		id相同，执行顺序从上到下；id不同，值越大，越先执行。在子查询中，越里面的id值越大。

​	**select_type**（查询类型）：表示这条查询的复杂度

​	**type**（访问类型）：

| type                | 含义                                    | 说明                              |
| ------------------- | --------------------------------------- | --------------------------------- |
| **system**          | 系统表，只有一行数据                    | 极少出现                          |
| **const**           | 常量查询，命中主键或唯一索引            | 最优！如 `WHERE id = 1`           |
| **eq_ref**          | 唯一索引扫描，JOIN 时使用主键或唯一索引 | 很好！如 JOIN ON a.id = b.id      |
| **ref**             | 非唯一索引扫描，返回匹配的多个行        | 好！如 `WHERE age = 18`（有索引） |
| **fulltext**        | 全文索引扫描                            | 少用                              |
| **ref_or_null**     | 类似 ref，但包含 NULL 值查询            | `WHERE age = 18 OR age IS NULL`   |
| **index_merge**     | 使用了多个索引的合并                    | 两个索引都用上了                  |
| **unique_subquery** | 子查询中使用唯一索引                    | `WHERE id IN (SELECT ...)`        |
| **index_subquery**  | 子查询中使用普通索引                    | `WHERE age IN (SELECT ...)`       |
| **range**           | 范围扫描，使用索引                      | `WHERE age > 18`（有索引）        |
| **index**           | 全索引扫描                              | 扫描了整个索引树，比全表稍好      |
| **ALL**             | **全表扫描**                            | ⚠️ **最差！必须优化！**            |

**优化目标**：至少达到 `range` 级别，最好 `ref` 或 `const`。

**possible_keys**（可能用到的索引）

- MySQL 认为查询可能用到的索引列表
- 如果为 NULL，说明没有可用索引

**key**（实际使用的索引）

- MySQL 最终决定使用的索引
- 如果为 NULL，表示没有使用索引

**key_len**（索引长度）

- 实际使用的索引字段的长度（字节数）
- 对于联合索引，可以判断用了哪些字段

```
-- 示例：联合索引 (name, age)
-- 如果 key_len = 100，可能只用了 name 字段
-- 如果 key_len = 108，可能用了 name + age 两个字段
```

**ref**（索引引用）

- 显示索引列和哪个值进行比较
- 常见值：`const`（常量）、`表名.字段名`（关联查询）	

**rows**（预估扫描行数）

- MySQL 预估需要扫描的行数（**不是精确值**，仅作参考）
- 这个值越小越好

**filtered**（过滤比例）

- 表示经过 `WHERE` 条件过滤后，剩余行数的百分比
- 100% 表示所有行都满足条件
- 值和 `rows` 相乘，得出实际匹配的行数

### **Extra（额外信息）**

包含 MySQL 执行查询的额外信息，常见值及含义：

| Extra 值                         | 含义                                 | 优化建议                       |
| -------------------------------- | ------------------------------------ | ------------------------------ |
| **Using index**                  | 覆盖索引（只查索引，不回表）         | ✅ 非常好！性能最优             |
| **Using where**                  | 用 WHERE 过滤数据                    | 正常，但要检查是否用了索引     |
| **Using index condition**        | 索引下推（ICP）                      | ✅ 好！MySQL 5.6+ 优化          |
| **Using filesort**               | ⚠️ 需要额外排序（文件排序）           | **需要优化**，加索引或改 SQL   |
| **Using temporary**              | ⚠️ 使用了临时表（如 GROUP BY 无索引） | **必须优化**，加索引或改 SQL   |
| **Using join buffer**            | 关联查询没走索引，用了缓冲区         | **需要优化**，给关联字段加索引 |
| **Impossible WHERE**             | WHERE 条件永远为假                   | 检查 SQL 逻辑                  |
| **Select tables optimized away** | 查询直接返回聚合结果（如 MIN/MAX）   | 极优！                         |

## 索引使用规则

SHOW INDEX FROM table；可以查看索引，其中联合索引的Seq_in_index列：

- 值是 **1**：该字段是索引的**第一列**（最左前缀列）
- 值是 **2**：该字段是索引的**第二列**
- 值越大，该列在索引中的优先级越低

### **最左前缀法则**

​	如果索引了多列(联合索引)，要遵守最左前缀法则。最左前缀法则指的是查询从索引的最左列开始，并且不跳过索引中的列。

​	**左边的列必须存在（但是顺序无所谓），否则全部失效，如果跳跃某一列，索引将部分失效(后面的字段索引失效)。**

**范围查询**

​	联合索引中，出现范围查询（>或者<），范围查询右侧的列索引失效。但是>=或<=是可以的。

### 索引失效

**1索引列运算**

​	explain select from tb_user where substring(phone,10,2) = '15';

​	即使对phone做了索引，还是会失效，导致全表扫描。

**2字符串不加引号**

**3模糊匹配**

​	如果仅仅是尾部模糊匹配，索引不会失效；如果是头部模糊匹配，索引失效。

**4or连接的条件**

​	用or分割开的条件，如果or前的条件中的列有索引，而后面的列中没有索引，那么涉及的索引都不会被用到。

**5索引效率小于全表扫描**

​	‘>=时大部分都满足， 。。。。一般都是返回的数据量比较大时，还不如全表扫描快。

### SQL提示

​	如果同时可以使用多条索引，可以通过一些提示来指定某个索引，来达到优化操作的目的。

​	都是在from表明后添加：

- ​	use index 索引名
- ​	ignore index 索引名
- ​	force index 索引名，强制指定，因为use index不一定被mysql采纳
- ​

### 覆盖索引

​	尽量使用覆盖索引（查询使用了索引，并且需要返回的列，在该索引中已经全部能够找到），减少select *。

​	看执行计划中的extra，using index condition：查找使用了索引，但是需要回表查询数据using where；usingindex：查找使用了索引，但是需要的数据都在索引列中能找到，所以不需要回表查询数据