

# 1

## 如何进行错误排查溯源的。

核心是通过一个traceId进行错误溯源的，这个traceId会贯穿ai客服的整个生命周期。

在接收到前端用户信息后，ai服务会读取请求头，看看有没有traceId，若没有生成一个uuid作为traceId，若有且合法则沿用。

一、通过traceId，先查ai_message，1如果没有任何消息，可以判断错误发生在消息落库前；2只有用户消息，说明创建助手任务时失败；3再根据助手消息的status，判断具体执行到了哪一步。

二、再通过traceId，查ai_tool_call，1如果没有工具记录，说明错误发生在工具调用前或者不需要工具；2如果有工具记录，再根据status判断具体是工具调用时进程崩溃了，还是工具调用的下游服务失败了，还是工具调用没问题，但是模型生成有问题。

三、如果是工具调用下游服务失败了，还可以查看错误原因，因为在ai服务跨服务调用sky-server后端服务时，会继续传递traceId，并在日志中记录调用结果。

因此完整排查方式是：

```
traceId
  ├─ 查 ai_message：定位 Agent 主流程阶段
  ├─ 查 ai_tool_call：定位具体工具和结果
  ├─ 查 sky-ai-service 日志：定位模型、编排或数据库异常
  └─ 查 sky-server 日志：定位内部鉴权和业务查询异常
```

## 如何保证agent不乱调用工具。

肯定不能仅仅通过提示词来希望模型遵守，而是要通过多层约束。

第一层是做意图和策略的分离，通过“意图管道”来识别意图，然后交给“决策机”来做权限决策，比如订单查询只授权`GET_ORDER_PROGRESS`工具，门店查询只授权 `GET_SHOP_STATUS`，退款这种写操作直接返回 `UNSUPPORTED，`退款这种写操作直接返回 UNSUPPORTED，根本不会进入 Agent。也就是说通过代码来给模型确定的权限。如果没有权限，模型就不知道其他工具的存在。相当于做了一个动态Tool的白名单。

第二层是敏感参数不交给模型控制。比如查询订单的 Tool 是通过无参数 Lambda 动态创建的：
()-> auditedOrderProgress(identity, request, assistantMessageId, traceId)
这里用户身份、订单请求、会话 ID 等信息在 Java 创建 Tool 时就已经绑定好了。模型只能决定“是否调用 get_order_progress”，不能自己传一个 orderId，更不能修改userId。

第三层是工具执行前的参数完整性校验和工具执行后的，如果调用工具时参数不完整，会直接抛异常，如果工具调用失败、返回 null 或业务结果不成功，也抛异常。同时每一次 Tool 调用都会记录工具名、结果状态、耗时等审计信息，方便追踪到底调用了什么。

## 如果模型选错 Tool 怎么办？  

首先他不可能每次都选对，但是能保证其不越权，因为每次开放给模型的工具都是按照意图分析来授权限制的。其次还会对调用后的结果做审核，如果工具调用数失败就直接中断，不允许返回正常结果。

## 你这个双令牌机制是做什么的？

AI 服务调用业务服务时，同时携带两个用途不同的内部 JWT。

第一个JWT是用户登录请求ai服务时携带的，ai服务可以通过这个jwt得到可信的userId。

第二个JWT是ai服务调用业务服务时携带的，业务服务可以通过这个jwt来确认调用这确实是ai服务，而不是普通用户或者伪造的请求。

Service JWT 证明“谁在调用”，User Context JWT 证明“这次调用代表哪个用户以及用于什么上下文”。两者由 AI Service 持有不同签名能力并由业务服务验证，用于分离服务身份和用户委托语义。

当前设计主要防外部伪造调用、普通 Header 冒充用户、token 跨用途复用和长时间重放，但不防 AI Service 本身完全失陷。如果要把 AI Service 也放在不可信边界之外，就需要让用户委托 token 由独立身份系统签发，AI Service 只能 exchange，不能自行签用户身份。

## 怎么确定userId可信？

orderId 从用户文本或上下文提取出来时始终是不可信的。服务端把它绑定到工具调用，只解决模型篡改问题，并不会让它自动可信。真正的信任边界在交易服务：交易服务从认证上下文拿到 userId，通过 orderId + userId 联合查询验证订单归属。只有查询成功后，这个订单引用才会被 AgentService 记录为 related_order_id。后续再次使用这个引用时仍然重新做归属校验，所以 related_order_id 是已验证上下文，不是授权凭证。

## 为什么 Redis 里的短期状态不会和数据库里的 `related_order_id` 冲突了怎么办？

当前 Redis 和 `related_order_id` 没有保存同一种数据，所以不存在正常路径上的双写一致性问题。已验证订单关联存在 MySQL，订单事实仍以订单服务数据库为准；Redis 只保存等待用户补参数这类可丢失的流程状态。我的判断标准是：Redis 丢了以后可以通过重新澄清恢复，但不能影响授权和业务正确性。Redis 故障时，显式订单查询、`related_order_id` 和权限校验都能继续，最多损失本轮短期槽位。正式环境里我会选择重新询问用户，而不是依赖多实例不一致的本地缓存。

# 2

## 



